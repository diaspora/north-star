<div class="pb-2 mt-5 mb-4 border-bottom">
  <h2>
    <a href="<%= blog_url(item) %>"><%= item[:frontmatter][:title] %></a>
    <small class="text-muted float-right"><%= format_date(item[:frontmatter][:date]) %></small>
  </h2>
</div>
